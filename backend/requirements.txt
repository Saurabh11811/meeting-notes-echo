# The product backend imports ASR, browser capture, and LLM provider modules at
# startup, so the repo-root requirements file is the single runtime dependency
# source for now.
-r ../requirements.txt
